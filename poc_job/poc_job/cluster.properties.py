###########################
# PASSWORD POLICY SETTINGS #
###########################

#Previous 5 passwords (configurable) (including the current password) cannot be reused.
PREVOIUS_PASSWORDS=5

#All passwords changed within 5 days (which must be configurable) cannot be reused.
PASSWORD_CHNGED_WITHIN_DAYS=5

#Configurable failed login attempts to be allowed (e.g. 5).
LOGIN_ATTEMPET=5

#Locked after 5 failed login attempts (which must be configurable) until the password is unlocked
LOGIN_ATTEMPET_LOCK=5

#90 days (which must be configurable) of password expiry for user account
PASSWORD_EXPIRY_DURATION=50
